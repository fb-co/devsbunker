// centralized place to control how many posts to load per increment for different sections of the app

const LoadAmounts = {
    mainPostFeed: 10,
    postSearch: 10,
    savedPosts: 10,
    personalPosts: 10,
};

export default LoadAmounts;