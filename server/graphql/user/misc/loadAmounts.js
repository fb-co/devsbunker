// Im adding this so we can have a signle file that will control all the queries loadAmount values
// *** this one only controls the user related fetching load amounts

const LoadAmounts = {
    userFeed: 1,
    userSearch: 1,
};

export default LoadAmounts;